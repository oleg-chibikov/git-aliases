#!/usr/bin/env bash
# Pull with rebase from the remote branch matching the current one.
set -euo pipefail
# shellcheck source=bin/_lib.sh
. "$(dirname "$0")/_lib.sh"

branch=$(git branch --show-current)
[ -n "$branch" ] || die 'detached HEAD, nothing to pull'

run git pull --rebase origin "$branch"
